/**
 * Block-editor entry.
 *
 * Mirrors Sage 11's `domReady` stub. Register blocks PHP-side (app/Blocks +
 * the block-handler); add editor-only JS below as needed.
 */
import domReady from "@wordpress/dom-ready";

domReady(() => {
	//
});
